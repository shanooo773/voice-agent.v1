[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Core dependencies for open-source voice agent
transformers = ">=4.35.0"
torch = ">=2.0.0"
scipy = ">=1.10.0"
numpy = ">=1.24.0"
sounddevice = ">=0.4.6"
soundfile = ">=0.12.1"
# Audio processing
pyaudio = "==0.2.14"
pydub = "==0.25.1"
speechrecognition = "==3.13.0"
# Hugging Face
huggingface-hub = ">=0.27.0"
safetensors = ">=0.4.0"
accelerate = ">=0.20.0"
# Gradio (optional - for UI)
gradio = "==5.12.0"
gradio-client = "==1.5.4"
# FastAPI (if needed for API)
fastapi = "==0.115.6"
uvicorn = "==0.34.0"
# Utilities
tqdm = ">=4.67.0"
pyyaml = ">=6.0.0"
requests = ">=2.32.0"
filelock = ">=3.16.0"
packaging = ">=24.0"

[dev-packages]

[requires]
python_version = "3.12"
